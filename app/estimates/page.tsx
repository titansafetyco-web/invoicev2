import DocList from "@/components/DocList";
export default function Page() {
  return <DocList title="Estimates" type="estimate" create="estimate" empty="No estimates yet. Create one to start the pipeline." />;
}
